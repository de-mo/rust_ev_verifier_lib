/*
pub struct File<T: FromJson> {
    location: Path,
    name: Name,
    data: Option<T>,
}

pub struct FileGroup<T: FromJson> {
    location: Path,
    name_search: Name,
    data: Option<T>,
}

pub struct Directory {
    path: Path,
    files: Vec<File>,
    file_groups: Vec<FileGroup>,
    directories: Vec<Directory>,
}

impl FileGroup {
    pub fn get_files() -> Vec<File> {}
}
 */
